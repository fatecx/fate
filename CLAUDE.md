# CLAUDE.md — fate

Narrative founder-saga game (fate.cx). Design phase — read `DESIGN.md` before any work; it is the source of truth for systems and content. Agent rules in `AGENTS.md` apply here in full.

## What this is

One persistent biography: you are a startup founder in a near-future world of AI-run corporations. Found three companies in fixed sequence — **HYPERCHUTE → TELEPORT → SKYLINE** — carrying reputation, relationships, cap table history, and scars across every death and exit. Citizen Sleeper is the quality bar: deterministic mechanics, authored narrative, constant near-scarcity.

## Non-negotiables

- Engine = pure deterministic TS; LLM renders prose from true state only (never outcomes/numbers).
- Content lives as typed data files; scenario-graph completeness is enforced by vitest.
- Game must be fully playable as text at all times — art never blocks.
- Runway / Stress / Reputation are the only meters.

## Current phase

**NEXT: Tauri desktop app.** The Tauri shell EXISTS and works (`src-tauri/`, Tauri v2, no plugins/IPC — the game is pure webview). Build: `source ~/.tokens && npx tauri build` → `src-tauri/target/release/bundle/macos/FATE.app` (arm64; `beforeBuildCommand` runs the full web build into `dist/`, which is bundled — offline-complete). Bundle target is `app` only: the tauri `dmg` script needs Finder-automation perms headless shells lack — make DMGs with `hdiutil create -srcfolder` (staging dir with FATE.app + /Applications symlink). Icons come from `npm run icons` (`scripts/gen-brand-icons.mjs` writes `src-tauri/icons/` incl. icon.icns — 824/1024 macOS plate, burnt paper + orbital spine; Arc-benchmark grid). **Desktop mode seam (`DESKTOP` in `src/app/main.ts`)**: inside Tauri (`__TAURI_INTERNALS__` or `tauri:` protocol) the app plays as a local founder (uid `desktop`, localStorage saves via `saveUid()`, no landing, no filing fee, `entitled=true`).

**LIVE on the buxor Vercel account** (user `buxord`, team `buxors` — zero Robomart/syedos connection). Ship: `npm run build && npm run deploy` (uploads dist via `VERCEL_TOKEN_BUXOR`; no GitHub↔Vercel integration). Current URL: **fate.cx** (aliased; apex DNS live). Split-stage play surface per DESIGN.md §8. **Auth is passkey-only — there is NO guest mode on the web**: WebAuthn passkey = the founder's signature ("Create your signature" / fingerprint). Server side: `/api/passkey` (Vercel function, `@simplewebauthn/server`) verifies registration/assertion statelessly (challenge tokens HMAC-signed with `FATE_AUTH_PEPPER`), maps each credential to a Supabase auth user whose password is `HMAC(pepper, user_id)` — released only after a verified assertion; the client then runs a normal `signInWithPassword`. Credentials live in the `passkeys` table (service-role only; RLS enabled, no client policies). Wallet-era sessions still label by their old address (`founderLabel` fallback) — their biographies survive. **The filing fee is $20 via Whop embedded checkout** (plan `plan_oXvp9t9S4YzeQ`, loader `js.whop.com/static/checkout/loader.js`, callback `data-whop-checkout-on-complete`); the receipt is verified server-side at `/api/whop` (Whop API key) and written to the `payments` table (PK user_id, UNIQUE tx — one receipt, one founder; pending receipts retry from localStorage via `claimPendingFee`). Vercel env (production): `FATE_AUTH_PEPPER`, `SUPABASE_SERVICE_ROLE`, `SUPABASE_URL`, `WHOP_API_KEY` (all also in ~/.tokens; service role fetchable via Supabase management API). **ALL crypto/wallet code is DELETED** (wallet.ts, pay.ts, @base-org/account, js-sha3 — 2026-08-31); the crash card still ignores errors thrown by users' browser extensions. **One passkey, one life — the biography is immutable.** No restart for players; the `saves` table has no client DELETE policy; replay = new passkey (revenue model + marketing claim: a lived-once experience). **Dev exception: `VITE_DEV_TOOLS=1` in `.env` bakes a RESTART (DEV) button into the drawer for playtesting — LAUNCH CHECKLIST: delete that var and redeploy.** In-fiction out: DECLARE BANKRUPTCY in the drawer (engine action `surrender` → chapter's `bankrupt` ending, scars carry forward, biography continues). Broken saves are fixed server-side, never via user resets. syedOS Render `fate-nb62.onrender.com` exists only as fallback.

Content status: **the full game — Hyperchute, Teleport, Skyline — is authored.** Hyperchute: three acts + survival register (`src/content/hyperchute/survival.ts`) drawn from the founder-owner's real early-startup stories. Teleport (`src/content/teleport/`): the expo arc (the quote, all-in booth, three checks at the show, the declined hotel card), three cofounders (player CEO + **Dr. Omid Farrokh** CTO purist + **June Park** cofounder-CFO who conditions her seed on operating again), Moon-only strategy (Mars killed in an authored war-room fight — HOW you kill it seeds Farrokh's loyalty), the 2.6-second **predictive-blend** honesty spine, the Jonah Reece handoff death (`t_transparent` golden thread), and the chapter's signature system: **the boardroom war** — board seats assembled across the whole chapter (June's seat, Priya vs the model's independent, ALEPH's rel-gated vote built by honest raw feeds), father's death → ALEPH's "leadership variance" memo → coup vote resolved by pure seat math (`src/content/teleport/preds.ts` SURVIVE_VOTE: two true allies kill the motion). New ending kind `ousted` ("REMOVED FOR CAUSE"). Dark roads are stress-CHEAP by design (the lie feels like air; it loses votes later); honest roads cost stress and win rooms. Skyline (`src/content/skyline/`): the strand discovery, Anders Voss's margin, Marcus Vale's full-circle alliance, sovereign money that becomes a country (**AURELIA**, Rashid + Volkov — venture-state arc: check → charter → flag → ministry offer), unmasked ALEPH ($2.1B anchor round, world-scoped respect carries from ch2), the uproot to FIRST RUNG, cargo-vs-people fork, the kilometer-921 tear (publish/quiet/bury — bury + cut margin opens the cable-fall road), the blockade, Calloway/Chen/the-Voss-siblings/Nadia-finale/Okonkwo scenes, and **the treaty vote by alliance math** (`skyline/preds.ts` WIN_TREATY: Okonkwo convinced + any 2 of Marcus/ALEPH/Chen/transparent). 8 endings: ascent (schoolteacher rides, hardest gate), eminent_domain, bankrupt (THE STUMP, also via refusing the seizure settlement), garrison, venture_state, port_authority, long_road, cable_fall. Witness roads for all 8 in economy.spec. Skyline reuses existing sound rooms; per-scene beds join when ElevenLabs quota renews (test scoped). all `world_s_*`/`cut_s_*`/`end_s_*`/`prologue_s_*` art generated in the locked ch1 risograph style. Enemy arc: MERIDIAN → your own cap table → nations. IPO ladder: 1 route (Hyperchute: the market ALWAYS refuses the first road show — `h_roadshow` ends in MERIDIAN torching pricing day; the only door to the bell is winning `h_last_stand` then `h_ipo_road` THE RETURN TO THE TABLE, gate transparent+score≥14+cred≥5+$100k+stress<85 — every triumph carries a refusal survived) → 2 proven doors (Teleport `t_listing`: allies-board road and clean-money no-ALEPH road, both witness-tested) → 4–5 (Skyline); rare endings proven by deterministic witness roads in `tests/economy.spec.ts`. Teleport art fully generated (169-frame riso batch; ox alpha's anime drift archived in `art/archive-oxalpha/`). Review surfaces: `map.html` (regenerated by `npm run build`, includes a SCRIPT tab with the full game text, and an **ART tab**: every frame in the game in a foldered gallery (left rail: CAST / chapter / ORPHANS / FLAGGED; chips filter by type; search by id/usage/character; click = full size). Flag frames for remake and SAVE FLAGS commits `art/flags.json` via `/api/save` action `flags` — the remake work order. ORPHANS lists files no content references. Full-res generation masters live in `art/masters-gen4/`) — hosted at **fate.cx/map** behind an AES-GCM passphrase gate (`scripts/map/encrypt.mjs`; passphrase is `FATE_MAP_PASS` in ~/.tokens, NEVER in code; unlock renders the decrypted page in a full-viewport `srcdoc` iframe — never Blob URLs (ugly address) or document.write (silently skips inline scripts)) — with a **browser editor**: the SCRIPT tab's EDIT mode makes every uniquely-locatable prose block contenteditable (build-time literal index `LOCK`/`FILEOF` in `scripts/map/generate.ts`; ~969/1,045 blocks; duplicate literals render locked); SAVE posts edits to **`/api/save`** (`api/save.ts`, Vercel function) which holds the GitHub token server-side (Vercel env `GITHUB_TOKEN` + `FATE_MAP_PASS`, set via `vercel env`) and pushes one commit to main — auth is the map passphrase itself (browser sessionStorage from the gate; NOTHING to paste), then the editor live-reports the ship run's status. **CI = `.github/workflows/ship.yml`**: every push to main runs `npm test` then vercel pull/build/deploy — bad edits fail tests and never ship. Repo secrets on fatecx/fate: `VERCEL_TOKEN`, `FATE_MAP_PASS`, `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`; the workflow also sets `VITE_DEV_TOOLS=1` (LAUNCH CHECKLIST: remove from ship.yml too). Same-machine alternative: `npm run script` → edit `script.md` → `npm run script:apply` (unique-literal write-back; rerun `npm test` + `npm run script` after). Gates: `npm test` (54, including tests/art.spec.ts: every referenced art id has a file and no scene ships imageless). **Conditional presence law:** prose may never hedge ("If June is in the room…") — use `SceneDef.vary` (first matching Pred overlays prose/leadIn/art at render); base prose must stand alone on every reachable path; hedges are banned by `tests/prose.spec.ts`. Character ids are canon-current (`anders`, `mateo`, `s_anders_margin`, `s_mateo` — no legacy `ingrid`/`talia` anywhere). Mei-Lin cut from cast: the accident victim is unnamed.

Chapter-close ceremony: endings may carry `screens` (full-screen film before the epilogue card — authored for `triumph_ipo`); the card leads to **THE RECORD** — stats, achievements (`ChapterDef.achievements`, Pred-evaluated), met-cast recap, and Walking-Dead-style community percentages (`ChapterDef.signatures`). Community data + the **Founders' Ledger** (`/leaderboard.html`, second vite page) live on buxor Supabase: tables `founders` (public read, own-row write) and `decisions` (own-row insert, read only via `decision_split(p_company)` security-definer RPC). Migrations run via management API `POST api.supabase.com/v1/projects/erwkzijhscmyqztifseb/database/query` with `SUPABASE_ACCESS_TOKEN_BUXOR` (~/.tokens). In-game: cast dropdown in the rail (faces unlock as met; shadows tease the unmet), and week marks stamp the bank balance (payroll auto-deducts weekly at epoch close — the drumbeat is visible, not real-time).

**Art recipe (character frames):** cast portraits are canon identity refs. When a named character appears in a scene frame, attach their portrait as an image ref BUT set `input_fidelity: "low"` and lead the prompt with the REF2 preamble ("the attached image defines the IDENTITY — compose the shot FRESH: different camera angle, head position, pose, expression; it is a casting photo, not a layout"). `input_fidelity: high` is BANNED for scene frames — it photocopies the portrait's exact face/angle into the scene (the June-in-the-diner bug; 24 frames were recast to purge it). Gold standard: `world_nadia.webp` (same person, new angle). The player founder NEVER shows a face — behind/silhouette only. Squares install via `cwebp -q 82 -resize 640 640`, wides via `cwebp -q 80`.

**Sound (three WebAudio decks, `src/app/audio.ts`):** MUSIC moves on BIG events only (`moodOf`: build→war→aftermath→endgame off act flags; films get `mus_film`), a ticking TENSION stem layers in when runway<10 / stress≥70 / arrears; AMBIENCE is diegetic room tone keyed by `SceneDef.ambience` (the unit is the PLACE — 11 rooms cover all scenes, test-enforced in `tests/sound.spec.ts`); STINGERS one-shot (bell on triumph, close on other endings, cut on cutscenes). Registry + generation prompts are data (`src/content/sound.ts`); files render to `public/sfx/` via `node scripts/audio/generate.mjs` (ElevenLabs sound-generation API, `ELEVENLABS_API_KEY` in ~/.tokens; free tier — attribution required for commercial use until upgraded; music API is paid-only, unused by design). Missing file = silence, never blocks. One SOUND ON/OFF row in the drawer; ignition on first gesture (autoplay law).
