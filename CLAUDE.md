# CLAUDE.md — fate

Narrative founder-saga game (fate.cx). Design phase — read `DESIGN.md` before any work; it is the source of truth for systems and content. Agent rules in `AGENTS.md` apply here in full.

## What this is

One persistent biography: you are a startup founder in a near-future world of AI-run corporations. Found four companies in fixed sequence — **HYPERCHUTE → TELEPORT → SKYLINE → ESCAPE** — carrying reputation, relationships, cap table history, and scars across every death and exit. Citizen Sleeper is the quality bar: deterministic mechanics, authored narrative, constant near-scarcity.

## Non-negotiables

- Engine = pure deterministic TS; LLM renders prose from true state only (never outcomes/numbers).
- Content lives as typed data files; scenario-graph completeness is enforced by vitest.
- Game must be fully playable as text at all times — art never blocks.
- Runway / Stress / Reputation are the only meters.
- Donor repo for auth/wallet scaffold: `~/.superset/projects/terminus` (read-only).

## Current phase

**LIVE on the buxor Vercel account** (user `buxord`, team `buxors` — zero Robomart/syedos connection). Ship: `npm run build && npm run deploy` (uploads dist via `VERCEL_TOKEN_BUXOR`; no GitHub↔Vercel integration). Current URL: **fate.cx** (aliased; apex DNS live). Split-stage play surface per DESIGN.md §8. **Auth is wallet-only — there is NO guest mode**: headless picker (Wallet Standard for Solana, EIP-6963 for Ethereum, no vendor modal) → `supabase.auth.signInWithWeb3` (SIWS/SIWE on buxor Supabase project `erwkzijhscmyqztifseb`; address/chain live in `user_metadata.custom_claims`). **One wallet, one life — the biography is immutable.** No restart for players; the `saves` table has no client DELETE policy; replay = new wallet (revenue model + marketing claim: a lived-once experience). **Dev exception: `VITE_DEV_TOOLS=1` in `.env` bakes a RESTART (DEV) button into the drawer for playtesting — LAUNCH CHECKLIST: delete that var and redeploy.** In-fiction out: DECLARE BANKRUPTCY in the drawer (engine action `surrender` → chapter's `bankrupt` ending, scars carry forward, biography continues). Broken saves are fixed server-side, never via user resets. syedOS Render `fate-nb62.onrender.com` exists only as fallback.

Content status: **Hyperchute, Teleport, and Skyline fully authored**; Escape remains a stub with an owner-approved outline. Hyperchute: three acts + survival register (`src/content/hyperchute/survival.ts`) drawn from the founder-owner's real early-startup stories. Teleport (`src/content/teleport/`): the expo arc (the quote, all-in booth, three checks at the show, the declined hotel card), three cofounders (player CEO + **Dr. Omid Farrokh** CTO purist + **June Park** cofounder-CFO who conditions her seed on operating again), Moon-only strategy (Mars killed in an authored war-room fight — HOW you kill it seeds Farrokh's loyalty), the 2.6-second **predictive-blend** honesty spine, the Jonah Reece handoff death (`t_transparent` golden thread), and the chapter's signature system: **the boardroom war** — board seats assembled across the whole chapter (June's seat, Priya vs the model's independent, ALEPH's rel-gated vote built by honest raw feeds), father's death → ALEPH's "leadership variance" memo → coup vote resolved by pure seat math (`src/content/teleport/preds.ts` SURVIVE_VOTE: two true allies kill the motion). New ending kind `ousted` ("REMOVED FOR CAUSE"). Dark roads are stress-CHEAP by design (the lie feels like air; it loses votes later); honest roads cost stress and win rooms. Skyline (`src/content/skyline/`): the strand discovery, Anders Voss's margin, Marcus Vale's full-circle alliance, sovereign money that becomes a country (**AURELIA**, Rashid + Volkov — venture-state arc: check → charter → flag → ministry offer), unmasked ALEPH ($2.1B anchor round, world-scoped respect carries from ch2), the uproot to FIRST RUNG, cargo-vs-people fork, the kilometer-921 tear (publish/quiet/bury — bury + cut margin opens the cable-fall road), the blockade, Calloway/Chen/the-Voss-siblings/Nadia-finale/Okonkwo scenes, and **the treaty vote by alliance math** (`skyline/preds.ts` WIN_TREATY: Okonkwo convinced + any 2 of Marcus/ALEPH/Chen/transparent). 8 endings: ascent (schoolteacher rides, hardest gate), eminent_domain, bankrupt (THE STUMP, also via refusing the seizure settlement), garrison, venture_state, port_authority, long_road, cable_fall. Witness roads for all 8 in economy.spec. Skyline reuses existing sound rooms; per-scene beds join when ElevenLabs quota renews (test scoped). all `world_s_*`/`cut_s_*`/`end_s_*`/`prologue_s_*` art generated in the locked ch1 risograph style. **Owner-approved outline for 4:** ESCAPE = the casino that funds the first civilian lunar town, AI crime syndicate, the founder's own health as the final fuse, everyone comes home, one secret ending earned only by a four-chapter-honest biography. Enemy arc: MERIDIAN → your own cap table → nations → time. IPO ladder: 1 route (Hyperchute `h_ipo_road`, transparent+score≥14+cred≥5+$100k+stress<85) → 2 proven doors (Teleport `t_listing`: allies-board road and clean-money no-ALEPH road, both witness-tested) → 4–5 (Skyline) → 6 (Escape); rare endings proven by deterministic witness roads in `tests/economy.spec.ts`. Teleport art fully generated (169-frame riso batch; ox alpha's anime drift archived in `art/archive-oxalpha/`). Review surface: `map.html` — regenerated automatically by `npm run build` (never stale; every content change reships it). Gates: `npm test` (52). **Conditional presence law:** prose may never hedge ("If June is in the room…") — use `SceneDef.vary` (first matching Pred overlays prose/leadIn/art at render); base prose must stand alone on every reachable path; hedges are banned by `tests/prose.spec.ts`. Character ids are canon-current (`anders`, `mateo`, `s_anders_margin`, `s_mateo` — no legacy `ingrid`/`talia` anywhere). Mei-Lin cut from cast: the accident victim is unnamed.

Chapter-close ceremony: endings may carry `screens` (full-screen film before the epilogue card — authored for `triumph_ipo`); the card leads to **THE RECORD** — stats, achievements (`ChapterDef.achievements`, Pred-evaluated), met-cast recap, and Walking-Dead-style community percentages (`ChapterDef.signatures`). Community data + the **Founders' Ledger** (`/leaderboard.html`, second vite page) live on buxor Supabase: tables `founders` (public read, own-row write) and `decisions` (own-row insert, read only via `decision_split(p_company)` security-definer RPC). Migrations run via management API `POST api.supabase.com/v1/projects/erwkzijhscmyqztifseb/database/query` with `SUPABASE_ACCESS_TOKEN_BUXOR` (~/.tokens). In-game: cast dropdown in the rail (faces unlock as met; shadows tease the unmet), and week marks stamp the bank balance (payroll auto-deducts weekly at epoch close — the drumbeat is visible, not real-time).

**Sound (three WebAudio decks, `src/app/audio.ts`):** MUSIC moves on BIG events only (`moodOf`: build→war→aftermath→endgame off act flags; films get `mus_film`), a ticking TENSION stem layers in when runway<10 / stress≥70 / arrears; AMBIENCE is diegetic room tone keyed by `SceneDef.ambience` (the unit is the PLACE — 11 rooms cover all scenes, test-enforced in `tests/sound.spec.ts`); STINGERS one-shot (bell on triumph, close on other endings, cut on cutscenes). Registry + generation prompts are data (`src/content/sound.ts`); files render to `public/sfx/` via `node scripts/audio/generate.mjs` (ElevenLabs sound-generation API, `ELEVENLABS_API_KEY` in ~/.tokens; free tier — attribution required for commercial use until upgraded; music API is paid-only, unused by design). Missing file = silence, never blocks. One SOUND ON/OFF row in the drawer; ignition on first gesture (autoplay law).
